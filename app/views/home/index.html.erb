  <h4 class="text-center">Witaj w Let's Bid It!</h4>
  <%= render 'kategories_table' %>
  <div id="myCarousel" class="carousel slide" data-ride="carousel">
    <!-- Indicators -->
    <ol class="carousel-indicators">
      <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarousel" data-slide-to="1"></li>
      <li data-target="#myCarousel" data-slide-to="2"></li>
    </ol>

    <h4 class="text-left">Może Cię zainteresować:</h4>
    <div class="row">
      <div class="col-xs-5">
        <div class="carousel-inner">
          <div class="item active">
            <a href="<%= aukcje_path(@aukcjes) %>">
              <b class="img-responsive"><%= image_tag @aukcjes.image.url(:medium) %></b>
              <div class="carousel-caption">
                <%= @aukcjes.nazwa %><br>
              </div>
            </a>
          </div>

          <div class="item">
            <a href="<%= aukcje_path(@aukcj) %>">
              <b class="img-responsive"><%= image_tag @aukcj.image.url(:medium) %></b>
              <div class="carousel-caption">
                <%= @aukcj.nazwa %><br>
              </div>
            </a>
          </div>

          <div class="item">
            <a href="<%= aukcje_path(@aukcje) %>">
              <b class="img-responsive"><%= image_tag @aukcje.image.url(:medium) %></b>
              <div class="carousel-caption">
                <%= @aukcje.nazwa %><br>
              </div>
            </a>
          </div>

          <!-- Left and right controls -->
          <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
      </div>
      <div class="col-xs-5">

      </div>

      </div>
      <div class="col-xs-2"></div>
    </div>
