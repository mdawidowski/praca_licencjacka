<div class="wrapper">
        <table class="table table-hover">
            <thead>
                <tr>
        <th>L.p</th>
        <th>Email</th>

                </tr>
            </thead>
            <tbody>
            <% @admin.order("created_at").each do |admin| %>
                <tr>
                    <td data-label="L.p"><%= admin.id%></td>
                    <td data-label="Email"><%= admin.email %></td>
                    <% end %>
            </tbody>
        </table>
    </div>
